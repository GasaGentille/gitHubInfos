import { GithubSearchDirective } from './github-search.directive';

describe('GithubSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new GithubSearchDirective();
    expect(directive).toBeTruthy();
  });
});
