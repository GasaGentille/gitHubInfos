import { RepoCreatedAtPipe } from './repo-created-at.pipe';

describe('RepoCreatedAtPipe', () => {
  it('create an instance', () => {
    const pipe = new RepoCreatedAtPipe();
    expect(pipe).toBeTruthy();
  });
});
